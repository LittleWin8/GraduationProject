<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.littlewin.system.mapper.UserAuthMapper">

    <select id="selectAdminLoginUser"
            resultType="com.littlewin.system.domain.dto.AdminLoginDTO">

        SELECT
            u.user_id    AS userId,
            u.status     AS status,
            a.identifier AS username,
            a.credential AS password
        FROM sys_user u
                 JOIN user_auth a ON u.user_id = a.user_id
        WHERE a.identifier = #{username}
          AND a.auth_type = 'password'
          AND u.user_type = 0
            LIMIT 1
    </select>

</mapper>