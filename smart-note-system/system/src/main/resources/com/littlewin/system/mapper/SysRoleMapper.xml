<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.littlewin.system.mapper.SysRoleMapper">
    <select id="selectRolesByUserId" resultType="com.littlewin.system.domain.entity.SysRole">
        SELECT r.*
        FROM sys_role r
                 LEFT JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
    </select>

    <select id="checkRoleKeyUnique" resultType="int">
        SELECT COUNT(1) FROM sys_role
        WHERE role_key = #{roleKey}
        <if test="roleId != null and roleId != ''">
            AND role_id != #{roleId}
        </if>
    </select>

    <select id="selectRoleIdsByUserId" resultType="long">
        SELECT role_id FROM sys_user_role
        WHERE user_id = #{userId}
    </select>
</mapper>