<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.littlewin.system.mapper.SysMenuMapper">

    <select id="selectMenuTree" resultType="com.littlewin.system.domain.entity.SysMenu">
        SELECT * FROM sys_menu
        ORDER BY sort_order ASC
    </select>

    <select id="selectPermsByRoleId" resultType="string">
        SELECT DISTINCT m.perms
        FROM sys_role_menu rm
                 INNER JOIN sys_menu m ON rm.menu_id = m.menu_id
        WHERE rm.role_id = #{roleId}
          AND m.perms IS NOT NULL
          AND m.perms != ''
    </select>

    <select id="selectMenuIdsByRoleId" resultType="long">
        SELECT menu_id FROM sys_role_menu
        WHERE role_id = #{roleId}
    </select>

</mapper>