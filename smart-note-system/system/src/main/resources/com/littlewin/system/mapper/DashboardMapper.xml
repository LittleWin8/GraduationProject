<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.littlewin.system.mapper.DashboardMapper">

    <!-- 总用户数 -->
    <select id="countTotalUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user WHERE del_flag = 0
    </select>

    <!-- 总笔记数（正常状态） -->
    <select id="countTotalNotes" resultType="java.lang.Long">
        SELECT COUNT(*) FROM note WHERE del_flag = 0 AND status = 1
    </select>

    <!-- 今日新增用户 -->
    <select id="countTodayNewUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user
        WHERE del_flag = 0 AND DATE(create_time) = CURDATE()
    </select>

    <!-- 今日新增笔记 -->
    <select id="countTodayNewNotes" resultType="java.lang.Long">
        SELECT COUNT(*) FROM note
        WHERE del_flag = 0 AND status = 1 AND DATE(create_time) = CURDATE()
    </select>

    <!-- 最近7天每天新增用户数 -->
    <select id="countNewUsersByDay" resultType="java.util.Map">
        SELECT DATE(create_time) AS date, COUNT(*) AS count
        FROM sys_user
        WHERE del_flag = 0
          AND DATE(create_time) BETWEEN DATE_SUB(CURDATE(), INTERVAL 6 DAY) AND CURDATE()
        GROUP BY DATE(create_time)
        ORDER BY date ASC
    </select>

    <!-- 最近7天每天新增笔记数 -->
    <select id="countNewNotesByDay" resultType="java.util.Map">
        SELECT DATE(create_time) AS date, COUNT(*) AS count
        FROM note
        WHERE del_flag = 0 AND status = 1
          AND DATE(create_time) BETWEEN DATE_SUB(CURDATE(), INTERVAL 6 DAY) AND CURDATE()
        GROUP BY DATE(create_time)
        ORDER BY date ASC
    </select>

    <!-- 笔记状态分布 -->
    <select id="countNotesByStatus" resultType="java.util.Map">
        SELECT status, COUNT(*) AS count
        FROM note
        WHERE del_flag = 0
        GROUP BY status
    </select>

    <!-- 热门笔记 TOP5 -->
    <select id="selectHotNotesTop5" resultType="java.util.Map">
        SELECT title, view_count AS viewCount
        FROM note
        WHERE del_flag = 0 AND status = 1
        ORDER BY view_count DESC
        LIMIT 5
    </select>

</mapper>
